<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Hello World</title>
</head>
<body>
#Example 1
    {{range .}}
        {{.}}
    {{end}}

#Example 2
    {{if .}}
            #Example 2 {{.}}
    {{end}}

#Example 3
    {{range .}}
        {{if .Name}}
            {{.Name}}
        {{end}}
    {{end}}

#Example 4
    {{range .}}
        {{if and .Name .Admin}}
            {{.Name}}
        {{end}}
    {{end}}
</body>
</html>